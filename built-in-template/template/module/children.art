import React, { FC } from "react";
import style from "./style.less";

interface {{childrenName}}Props {
  name: string;
}

const {{childrenName}}: FC<{{childrenName}}Props> = (props) => {
  const { name } = props;

  return (
    <div className={style.wrapper}>
      <p className="p">This children name is <span>{`${name}`}</span></p>
    </div>
  );
};

export { {{childrenName}} };
