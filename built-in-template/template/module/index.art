import React, { FC } from "react";
{{each childrenNameList}}
import { {{$value}} } from "./{{$value}}";
{{/each}}

import style from "./style.less";

const {{moduleName}}: FC = () => {
  return (
    <div className={style.wrapper}>
      <h2 className="h2">Hello Echoo, 这是一个模块哦</h2>

      <p>This page is {{moduleName}}</p>
      {{each childrenNameList}}
      <{{$value}} name="{{$value}}" />
      {{/each}}
    </div>
  );
};

export { {{moduleName}} };
